using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ThrowingChuck : MonoBehaviour {

    //void fire() {
    //    Debug.Log("ThrowingChuck threw a ball.");
    //    Projectile p = Instantiate(projectileprefab, projectileSpawnpoint.position, projectileSpawnpoint.rotation);

    //    p.tag = "Projectile_Enemy";
    //    p.GetComponent<SpriteRenderer>();
    //}

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
